[pytest]
; No real need to register this marker; the decorator suffices
markers =
    by_inspection: mark a test as a check by inspection; always successful as far as pytest is concerned
addopts = -m "not by_inspection"
;addopts = -m "by_inspection"